name: byterover
version: 1.0.0
description: "ByteRover — 通过 brv CLI 实现分层检索的持久化知识树。"
external_dependencies:
  - name: brv
    install: "curl -fsSL https://byterover.dev/install.sh | sh"
    check: "brv --version"
hooks:
  - on_pre_compress
